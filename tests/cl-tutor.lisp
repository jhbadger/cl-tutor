(defpackage cl-tutor-test
  (:use :cl
        :cl-tutor
        :prove))
(in-package :cl-tutor-test)

;; NOTE: To run this test file, execute `(asdf:test-system :cl-tutor)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
